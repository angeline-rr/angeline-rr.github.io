let missilX = parseInt(missil.style.left);
let missilY = parseInt(missil.style.top);
let missilW = missil.offsetWidth;
let missilH = missil.offsetHeight;

let alienX = parseInt(alien.style.left);
let alienY = parseInt(alien.style.top);
let alienW = alien.offsetWidth;
let alienH = alien.offsetHeight;

// Verificação de colisão manual (AABB - Axis-Aligned Bounding Box)
if (
    missilX < alienX + alienW &&
    missilX + missilW > alienX &&
    missilY < alienY + alienH &&
    missilY + missilH > alienY
) {
    // Colisão detectada
}

position absolute se posiciona de acordo com o ancestral position relative mais perto
position relative é deslocado em relação a si mesmo